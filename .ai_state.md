# Project Objective: Improve the csl-guides Docusaurus site

Acted on a four-bundle audit (accuracy, content depth, UX, infra), end-user weighted.
Bundles 1–4 are in `main` via PR #1. PR #2 (AP90 screenshot query fix) is open. The three
follow-up extras are on branch `feature/guides-extras`.

## ➡️ Next Steps (Queue)

- Merge PR #2 (AP90 `agniH`) and the `feature/guides-extras` PR.
- Optional, still not done: deeper accent/Vedic guidance (D5).

## 🚧 Current Work-In-Progress (WIP)

- (none)

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- Original `@theme/SearchPage` build error = stale `.docusaurus` cache; fixed with
  `npm run clear` + restart. search-local 0.55.2 was fine.
- Version is single-sourced: `src/data/dictionaries.json` `siteVersion` (scraped by
  `scripts/build-catalog.mjs`) → `src/components/SiteVersion.js` → used in `.mdx` pages.
- Screenshots: shared `src/components/Screenshot.js` (intrinsic dims + lazy). Capture via
  `node scripts/screenshots.mjs [nameFilter]`; live server needs a browser UA (403s bots).
- Build verification: `npm run build` (onBrokenLinks: 'throw'); stop the dev server first.
- `LICENSE` = full canonical CC BY-SA 4.0 legalcode (GitHub-detectable). CoC contact is a
  GitHub channel, not a personal email — maintainer may want a dedicated address.

## ✅ Completed (Recent only)

- **Bundle 1 — quick wins** (`5deaea7`): A1 version single-source; A2 śiva/rāma
  transliteration contrast; A3 FAQ API/GraphQL wording; D7 README status refresh.
- **Bundle 2 — end-user content** (`2c2c7a9`): D1 Quick Start; D2 front-page screenshot +
  shared Screenshot component; D3 Multi-Dictionary worked example; D4 cross-dictionary
  comparison (wired the AP90/PWG/GRA shots + committed the screenshots.mjs WIP); D6
  Troubleshooting page; U5 catalog mobile scroll; sidebar updates.
- **Bundle 3 — infra & governance** (`d1ff621`): I1 LICENSE (CC BY-SA 4.0 everywhere),
  CONTRIBUTING, CODE_OF_CONDUCT; I5 build-on-PR CI; I2 weekly external link check;
  I3 browser UA for the catalog refresh.
- **Bundle 4 — second accuracy pass**: fixed SLP1 of kṛṣṇa (`kfRZa`→`kfzRa`) in
  change-files.md; corrected csl-app from "Web frontend" to "Cross-platform app" in
  repositories.md (verified via GitHub). Rest of Contributing/Developers/About verified
  accurate (conventions match; all referenced repos exist; csl-app stack confirmed Dart/Flutter).
- **Follow-up extras** (`feature/guides-extras`): live `dalglob` shot — captured the
  dalglob1 sample (agni across dictionaries) and rewrote Multi-Dictionary to describe it
  accurately (dalglob.php = JSON key→dicts layer; sample/dalglob1.php = the display);
  social card (`static/img/social-card.png`, set as `themeConfig.image`); dark-mode matte
  for screenshots (`.csl-shot` class + `[data-theme='dark']` CSS).
