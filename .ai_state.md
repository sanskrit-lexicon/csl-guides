# Project Objective: Improve the csl-guides Docusaurus site

Audience priority (per maintainer): **Developers** first, then MW-style **deep pages for the
big dictionaries**. English-only (no i18n this round). Tone = **canonical** — every factual
claim verified against a real source file, not asserted.

## ➡️ Next Steps (Queue)

- Optional: link the catalog table rows to the featured deep pages (DictionaryCatalog.js has
  no per-page link today; pages are reached via the sidebar + cross-links only).
- Optional: more featured deep pages (SKD/VCP Sanskrit→Sanskrit, BHS, the Russian group).
- Optional, still open from before: deeper accent/Vedic guidance (D5).

## 🚧 Current Work-In-Progress (WIP)

- (none) — PR #1 merged (#22); PR #2 pushed.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- **Link-check CI fix** (PR #22, verified green on real CI): lychee fails on Docusaurus
  root-relative routes at *extraction* time, before `--exclude`/`--scheme` run. Fix =
  `--root-dir "${{ github.workspace }}"` (extraction succeeds → file: URLs → `--scheme
  https/http` drops them) + `--accept "200..=299, 429"` (tolerate github rate-limits). It
  only runs on schedule/`workflow_dispatch` — verify a fix by dispatching, not assuming.
- **NO catalog PW bug** (earlier hypothesis was WRONG, verified): `build-catalog.mjs` already
  has `REPO_OVERRIDES = {MW:'MWS', AE:'ApteES', PW:'PWK'}` and the committed
  `dictionaries.json` already has PW `repo=PWK`. MWE is intentionally in `NO_REPO`.
- **PW vs PWK**: dictionary CODE = `PW` (URLs, `csl-orig/v02/pw`); GitHub REPO = `PWK`.
- **MWE markup**: body records ARE `<L>…<LEND>` (the `<H>` lines are title-page front
  matter). English headword in `<k1>`; `{%…%}` = English POS/sense labels; `{#…#}` =
  Sanskrit equivalents (SLP1). English→Sanskrit (reverse of MW).
- **Deep-page wiring**: sidebars.js `developersSidebar`→ no; it's `dictionariesSidebar` →
  "Featured dictionaries" items. Catalog table does NOT link to deep pages.
- **Screenshots** available only for pwg/ap90/gra (`static/img/displays/{code}-basic.png`,
  1800x1350); PW & MWE have none — those pages omit the Screenshot component.
- **Blog permalink gotcha**: a post with `slug: X` is at `/news/X`, NOT the date path.
- Build verification: `npm ci && npm run build` (onBrokenLinks:'throw'); green as of PR #2.

## ✅ Completed (Recent only)

- **PR #2 — dictionary deep pages** (branch `improve/dictionary-deep-pages`, off main):
  new `docs/dictionaries/{pwg,pw,ap90,gra,mwe}.mdx` in the `mw.mdx` mold (at-a-glance table,
  when-to-use, reading-an-entry with a REAL verified source record, distinctive features,
  see-also); added to `sidebars.js` "Featured dictionaries"; cross-linked from `mw.mdx`
  complements and `dictionaries/overview.md`. Build green.
- **PR #1 — Developers deepening + link-check fix** — MERGED as #22 (`7ec2bba` on main):
  deepened all 5 developer pages (api/data-formats/architecture/generation-pipeline/
  repositories), fixed the link-check workflow (verified green: 0 err, 121 ext, 154
  internal excluded), and repaired a real dead DCH link in `about/publications.md`.
