# Project Objective: Improve the csl-guides Docusaurus site

Acting on a four-bundle audit (accuracy, content depth, UX, infra), end-user weighted.
Work happens on branch `improve/guides-audit-2026-06-13`, committed per bundle.

## ➡️ Next Steps (Queue)

- **Bundle 2 — End-user content**: quick-start page (D1); front-page screenshot (D2);
  multi-dictionary worked example (D3); wire the AP90/PWG/GRA basic shots into a
  "same word across dictionaries" comparison + commit the pending screenshots.mjs WIP (D4);
  "no results?" troubleshooting aid (D6).
- **Bundle 3 — Infra & governance**: CONTRIBUTING.md, LICENSE, CODE_OF_CONDUCT.md (I1);
  external link-check workflow (I2); build-on-PR job (I5); realistic UA for the CI catalog
  refresh (I3).
- **Bundle 4 — Second accuracy pass**: fix SLP1 of kṛṣṇa in change-files.md (`kfRZa`→`kfzRa`);
  finish verifying Contributing/Developers/About against the org repos.

## 🚧 Current Work-In-Progress (WIP)

- (none mid-edit; Bundle 1 committed)

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- The original `@theme/SearchPage` build error was a stale `.docusaurus` cache, fixed by
  `npm run clear` + restart (search-local 0.55.2 was fine).
- Version dedup (A1): single source is `src/data/dictionaries.json` `siteVersion`, scraped
  by `scripts/build-catalog.mjs`, surfaced via `src/components/SiteVersion.js`. Pages that
  showed the version were renamed `.md`→`.mdx` so they can import the component.
- `scripts/screenshots.mjs` + `static/img/displays/{ap90,gra,pwg}-basic.png` were pre-existing
  uncommitted WIP carried onto this branch — they belong to Bundle 2 (D4), not Bundle 1.
- Build verification: `npm run build` is the authoritative check (`onBrokenLinks: 'throw'`).
  Stop the dev server first so it doesn't race on `.docusaurus`.

## ✅ Completed (Recent only)

- **Bundle 1 — Quick wins** (build-verified):
  - A2: transliteration table now contrasts *rāma* (identical in slp1/hk/itrans) with
    *śiva* (`Siva`/`ziva`/`shiva`) + an explanatory tip — `docs/users/encoding-transliteration.md`.
  - A3: corrected the FAQ API/GraphQL wording — `docs/faq.md`.
  - D7: refreshed the stale README "Status" section (no more bogus `TODO(verify)` hunt).
  - A1: version single-sourced via `SiteVersion` component + `siteVersion` in catalog data.
