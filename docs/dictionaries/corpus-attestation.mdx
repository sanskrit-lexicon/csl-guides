---
id: corpus-attestation
title: How Often Is This Word Actually Used?
description: "The corpus attestation layer: Digital Corpus of Sanskrit frequency counts joined to dictionary headwords — 4.55 million tokens over 59,282 lemmas, with per-period profiles."
sidebar_label: Corpus attestation (data layer)
---

import CorpusFrequencyTop, {CorpusCoverage, PeriodProfiles} from '@site/src/components/CorpusFrequency';
import VedicAccentTop, {VedicAccentStats, VedicAccentLemmas} from '@site/src/components/VedicAccent';

# How Often Is This Word Actually Used?

A dictionary is deliberately democratic: a word attested once in a single late
commentary gets an entry formatted exactly like *ca* or *bhū*. That is what makes
dictionaries complete — and what makes them silent about **usage**. This page adds the
missing signal: corpus attestation counts from Oliver Hellwig's
[Digital Corpus of Sanskrit](http://www.sanskrit-linguistics.org/dcs/) (DCS, CC BY),
a lemmatized, human-validated corpus. The feed used here counts **4,550,704 tokens
over 59,282 lemmas**.

## Sanskrit is extremely top-heavy

A small core of lemmas does most of the work in real texts:

<CorpusCoverage />

Two thousand lemmas — roughly one percent of Monier-Williams' 185,803 entries —
account for over three quarters of all corpus tokens. For a learner this is the
strongest prioritization signal there is: the
MW reading guide teaches you *how* to read an entry;
this layer tells you *which* entries you will actually meet.

## The most frequent lemmas

<CorpusFrequencyTop n={25} />

POS tags are the DCS grammar codes (`ind` indeclinable, `pron` pronoun, `m`/`f`/`n`
noun by gender, `adj` adjective, `1.P.`-style codes for verb roots by class and pada).
Every lemma is keyed in SLP1, so it joins directly
against Cologne headword keys.

## Most headwords are corpus-rare

Only 59,282 lemmas carry a whole-corpus count (83,277 appear in the source layer at
all) — against 185,803 MW entries. The gap is not an error: large dictionaries carry
enormous tails of words inherited from the indigenous lexicographic tradition (MW's
"L." = *lexicographers* marker; see the SKD and
VCP kośa pages), attested rarely or only in lexica. When a
dictionary entry cites only lexicographers and the corpus count is zero, you are
looking at a word of the dictionary tradition rather than of surviving usage — a
distinction no dictionary layout shows you, but the corpus layer does.

## Words with a history

The feed carries a per-period vector for each lemma (DCS's chronological buckets —
numbered slots ending at an approximate year, e.g. `1 -800` ≈ texts up to 800 BCE,
`5 1200` ≈ up to 1200 CE; the labels `3200` and `4700` are slots 3 and 4 — up to
200 CE and 700 CE — with the space lost in the upstream export; plus the undatable
genre buckets `9 Vedic`, `11 Epic`, `12 Classic`). Frequency profiles differ
dramatically:

<PeriodProfiles lemmas={['Uti', 'rayi', 'vfzan', 'vESampAyana', 'vEdehI', 'pArada', 'ganDaka', 'aBraka']} />

- ***ūti* "help, favour", *rayi* "wealth", *vṛṣan* "bull, mighty"** — Vedic workhorses
  that almost vanish afterwards. For these, Grassmann (a dedicated
  Rigveda dictionary with complete attestation) serves you better than any
  general-purpose dictionary.
- ***Vaiśampāyana*, *Vaidehī*** — epic names concentrated in the `11 Epic` bucket:
  Mahābhārata/Rāmāyaṇa reading is PWG and MW
  territory, both of which cite the epics massively (see
  what each dictionary quotes).
- ***pārada* "mercury", *gandhaka* "sulphur", *abhraka* "mica"** — alchemical
  (rasaśāstra) vocabulary whose counts sit in the late buckets. Late technical Sanskrit
  is real Sanskrit too, and its vocabulary barely overlaps the poetic core.

The same lemma-level profiles power the corpus-era view in the
dictionary landscape page's date reasoning: which dictionary
to open depends on *when* your text was written.

## The same words in the accented Rigveda

Classical learners meet Sanskrit stripped of its pitch accent; the
encoding guide explains udātta notation abstractly.
This layer shows **real attested accented word-forms** for lemmas you may already know from
the frequency work above — drawn from the VedaWeb bulk export of the Zurich Rigveda
annotation corpus (Casaretto et&nbsp;al. 2025, CC BY 4.0), whose word-split carries an
udātta mark on every accented syllable:

<VedicAccentStats />

Under half of today's most frequent Classical vocabulary is already attested in the
Rigveda slice (845 of 2,000 joined lemmas carrying 45.3% of all DCS tokens) — and where a
lemma does appear, seeing its accent live teaches what no paradigm table quite can:

<VedicAccentTop n={20} />

A few of those with room to breathe:

<VedicAccentLemmas lemmas={['kf', 'agni', 'vAc', 'brAhmaRa']} />

Even a single glance shows placement variety that printed paradigms flatten: *vā́k* has its
udātta on the stem vowel while the locative *vācí* shifts it rightward, and *agni*'s vocative,
theme-vowel and accusative shapes carry the mark in three different places (`ágne`, `agne`,
`agním`).

:::note Trust block
- **Evidence:** [`src/data/corpus-frequency.json`](https://github.com/sanskrit-lexicon/csl-guides/blob/main/src/data/corpus-frequency.json)
  (top 2,000 lemmas by corpus rank + whole-corpus stats), regenerated by
  [`scripts/build-corpus-frequency.mjs`](https://github.com/sanskrit-lexicon/csl-guides/blob/main/scripts/build-corpus-frequency.mjs)
  from [`kosha/data/frequency/lemma_frequency.tsv`](https://github.com/gasyoun/kosha/blob/main/data/frequency/lemma_frequency.tsv)
  (83,277 lemmas), itself built from the VisualDCS M9 archive of DCS data. Upstream:
  Digital Corpus of Sanskrit (Oliver Hellwig), **CC BY**. The *Vedic accents* section adds
  [`src/data/vedic-accent.json`](https://github.com/sanskrit-lexicon/csl-guides/blob/main/src/data/vedic-accent.json),
  regenerated by
  [`scripts/build-vedic-accent.mjs`](https://github.com/sanskrit-lexicon/csl-guides/blob/main/scripts/build-vedic-accent.mjs)
  (SHA-256 edge contract over the upstream bytes, `--check` mode) from the landed VedaWeb
  bulk export — the one-time acquisition in
  [VisualDCS/non-derived/vedaweb](https://github.com/gasyoun/VisualDCS/tree/main/non-derived/vedaweb)
  ([H096](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H096-Sonnet_VisualDCS_vedaweb_feed_export_03.07.26.md),
  08-07-2026); this site never queries `vedaweb.uni-koeln.de` directly. Every table and
  percentage on this page is computed from the committed feeds at build time — nothing is
  hand-typed.
- **Limitations:** the frequency tables see the **top-2,000 slice**, not all 83,277
  lemmas. DCS is genre-skewed (its later periods are dominated by śāstric and
  alchemical texts, which is why the "late-heavy" examples are rasaśāstra terms —
  a corpus fact, not a claim about all late Sanskrit). Period bucket boundaries are
  DCS-coded and approximate (the upstream
  [QL layer README](https://github.com/gasyoun/VisualDCS/blob/main/derived-data/QL/README.md)
  flags the label-to-boundary mapping as not verified cell-by-cell). Counts are
  lemmatized tokens from DCS's analyses — a different corpus would give different
  numbers. The **Vedic join is a lower bound**: RV lemma citation forms differ from
  Classical ones for a real tail of stems (e.g. the pronouns cited as *sá- ~ tá-* where
  the corpus layer keys *tad*), so lemmas can be RV-attested yet missing from the join;
  accent marks are stripped only for keying and always verbatim on display.
- **Rights:** DCS is CC BY; the feed is a derived aggregation with attribution
  (evidence record §1 in
  [`NON_COLOGNE_SOURCES.md`](https://github.com/sanskrit-lexicon/csl-guides/blob/main/src/data/NON_COLOGNE_SOURCES.md)).
  The vedic-accent feed: Casaretto et&nbsp;al. (2025)/VedaWeb, Universität zu Köln,
  **CC BY 4.0** (rights confirmed per H359; evidence record §3 in the same file).
- **Owner repos:** DCS (upstream) → [VisualDCS](https://github.com/gasyoun/VisualDCS) →
  [kosha](https://github.com/gasyoun/kosha) (frequency layer) → this repo (rendering);
  VedaWeb bulk export → VisualDCS `non-derived/vedaweb` → this repo.
- **Built by:** Fable 5 (`claude-fable-5`), 07-07-2026, handoff
  [H280](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H280-Fable_csl-guides_guides_stream3_data_layer_sections_07.07.26.md);
  Vedic-accent section by OxAlpha (`opencode/x-preview-f-free`), 26-08-2026.
:::

## See also

- How machines read Sanskrit — the morphology & segmentation layer
- The dictionary landscape — era × novelty × size for all 43 dictionaries
- What each dictionary quotes — the citation-frequency layer
- Reading Monier-Williams — entry structure, symbols, dhātu-tracing
