# Project Objective: Improve the csl-guides Docusaurus site

Audience priority (per maintainer): **Developers** first, then MW-style **deep pages for the
big dictionaries**. English-only (no i18n this round). Tone = **canonical** — every factual
claim verified against a real source file, not asserted.

## ➡️ Next Steps (Queue)

- **PR #2 — dictionary deep pages** (PWG, PW, AP90, GRA, MWE) in the `mw.mdx` mold.
  Facts already gathered & verified: PWG/AP90/GRA (source samples + markup quirks), PW
  (code `PW`, repo `PWK`, `csl-orig/v02/pw`), MWE (1851, English→Sanskrit, `<H>` headword
  markup — grab one real lexical entry when writing). Wire each from the catalog.
- Optional: fix the catalog data bug — `dictionaries.json` gives PW `repoUrl=.../PW` which
  404s; the real repo is `PWK`. Fix in `scripts/build-catalog.mjs` (code→repo map).
- Optional, still open from before: deeper accent/Vedic guidance (D5).

## 🚧 Current Work-In-Progress (WIP)

- (none) — PR #1 pushed.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- **Link-check CI was permanently red**: lychee can't resolve Docusaurus root-relative
  routes (`/news`, `/developers/api`) as local files ("provide a root dir"). Those are
  already validated by the build (`onBrokenLinks: 'throw'`). Fixed with `--exclude "^/"`.
- **PW vs PWK**: dictionary CODE = `PW` (URLs, `csl-orig/v02/pw`, clean-URL whitelist);
  GitHub REPO = `PWK`. No `sanskrit-lexicon/PW` repo (404).
- **API ground truth** (csl-apidev/doc + source): live native actions = listview, listhier,
  getword, getsuggest, servepdf; getword_xml = alpha; salt_* + cleanurl = roadmap (PR #46 /
  COLOGNE#249). CORS `*` + JSONP `callback` are REAL in the live endpoint .php files
  (not just api_trial.php). No auth / no rate limit. 404 on unknown dict code.
- **Generation pipeline** (csl-pywork/v02/readme.md): generate_dict.sh = 4 stages
  (orig → pywork → web → execute); stage-4 redo_hw/redo_xml/redo_postxml produce
  hw.txt / {dict}.xml / sqlite. Four displays B/L/A/M = webtc/webtc1/webtc2/mobile1.
- Build verification: `npm ci && npm run build` (onBrokenLinks:'throw'); green as of this PR.

## ✅ Completed (Recent only)

- **PR #1 — Developers deepening + link-check fix** (branch `improve/developers-and-dicts`):
  - `link-check.yml`: `--exclude "^/"` so the weekly job stops failing on internal routes.
  - `developers/api.md`: live-vs-roadmap status table; full request-parameter reference;
    worked examples per native action; CORS/JSONP, errors, no-auth/no-rate-limit; tightened
    Salt + clean-URL sections (all verified against csl-apidev/doc + source).
  - `developers/data-formats.md`: full tag set; brace conventions ({#..#}/{%..%}/{@..@});
    lnum semantics; generated-XML structure (one.dtd); SQLite/JSON/StarDict/interop formats.
  - `developers/architecture.md`: the four web displays (webtc/webtc1/webtc2/mobile1) + SQLite.
  - `developers/generation-pipeline.md`: the 4-stage build + artifact table.
  - `developers/repositories.md`: csl-sqlite/csl-json/cologne-stardict promoted; PW-vs-PWK note.
  - Verified: `npm run build` green (no broken internal links).
